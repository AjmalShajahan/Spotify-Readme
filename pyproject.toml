[project]
name = "spotify-readme"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.3",
    "python-dotenv==1.2.2",
    "requests==2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
